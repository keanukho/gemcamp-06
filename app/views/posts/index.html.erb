<%# @posts.each do |post| %>
  <%#=post.title%>
  <%#=post.content%>
<%# end %>


<h1>Post List</h1>

<table class="table table-success table-striped">
  <thead>
  <td>title</td>
  <td>content</td>
  <td>action</td>
  </thead>
  <% @posts.each do |post| %>
    <tr>
      <td><%= post.title %></td>
      <td><%= post.content %></td>
      <td></td>
    </tr>
  <% end %>
</table>